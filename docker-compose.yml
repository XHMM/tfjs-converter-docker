version: "3"

services:
  convert:
    container_name: convert
    build:
      context: .
    environment:
      - test=hi
    env_file:
      - .env
    volumes:
      - ./models:${in}
      - ./converted:${out}
